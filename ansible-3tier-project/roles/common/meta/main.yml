# ============================================================
# roles/common/meta/main.yml — Role Metadata
# TOPIC COVERED: Role metadata, Ansible Galaxy
# ============================================================
galaxy_info:
  author: YourName
  description: "Common server baseline configuration"
  company: HCL Technologies
  license: MIT
  min_ansible_version: "2.12"
  platforms:
    - name: Ubuntu
      versions: ["20.04", "22.04"]
  galaxy_tags:
    - system
    - security
    - baseline

# Dependencies: other roles that must run BEFORE this one
dependencies: []  # common has no dependencies
